#!/usr/bin/env bash

# maximize window (using my hotkey)
osascript -e '
tell application "System Events"
    key code 36 using {control down, option down}
end tell
'

tmux set-option -g status
cmatrix
tmux set-option -g status

# position window to the left (using my hotkey)
# Disabling it as it will better to restore instead of positioning to a specific position
# osascript -e '
# tell application "System Events"
#     key code 124 using {control down, option down}
# end tell
# '
