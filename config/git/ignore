# Compiled Python files
*.pyc

# Folder view configuration files
.DS_Store
Desktop.ini

# Thumbnail cache files
._*
Thumbs.db

# Files that might appear on external disks
.Spotlight-V100
.Trashes

**/.claude/settings.local.json

# Claude Code per-project working state (see ~/.claude/CLAUDE.md memory protocol)
.agent/

# Project-local agent skills vendored by "npx skills" (never commit)
.agents/
